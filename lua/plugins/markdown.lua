return {
  "MeanderingProgrammer/render-markdown.nvim",
  opts = {
    heading = {
      sign = false,
      icons = {},
      backgrounds = {}, -- no full-line background bars; headers stay distinct via foreground color only
    },
  },
}
